@*@model IEnumerable<XCLHMS.Models.MLC>*@

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<style>
    .th-width {
        width: 32px !important;
    }
</style>
<div style="font-family:Arial">
    <h2>MLC List</h2>
    <p style="float:right;">
        @Html.ActionLink("Add New", "Create", null, new { @class = "btn btn-primary btn-large btnAdd" })
    </p>
    <div class="container">
        <br />
        <div style="width:100%; margin:0 auto;">
            <table id="demoGrid" class="table table-striped table-bordered dt-responsive nowrap" width="100%" cellspacing="0">
                <thead>
                    <tr>
                        <th>MLC #</th>
                        <th>Patient Name</th>
                        <th>Dated</th>
                        <th>MLC Type</th>
                        <th>Age</th>
                        <th>Gender</th>
                        <th>Police Letter</th>
                        <th class="th-width"></th>
                        <th class="th-width"></th>
                        <th class="th-width"></th>
                        <th class="th-width"></th>
                    </tr>
                </thead>
            </table>
        </div>
        @Html.Hidden("hdnPageSecurity", @XCLHMS.Helpers.SessionHelper.GetPageSecurity())
       
    </div>   
</div>
@section Scripts{
    <script>
        $(document).ready(function () {
            $('#demoGrid').DataTable({
                "ajax": {
                    "url": "/MLC/LoadGrid",
                    "type": "GET",
                    "datatype": "json"
                },
                "fnRowCallback": function (nRow, aData, iDisplayIndex) {

                    var arr1 = jQuery('#hdnPageSecurity').val().split('|');
                    var _valIsAdd = arr1[0];
                    var _valIsEdit = arr1[1];
                    var _valIsDelete = arr1[2];
                    if (_valIsAdd == '1') {
                        $('.btnAdd').removeAttr('disabled');

                    }
                    else {
                        $('.btnAdd').attr('disabled', 'disabled');
                    }

                    if (_valIsEdit == '1') {

                        $('td:eq(8)', nRow).find('a').removeAttr('disabled');
                    }
                    else {
                        $('td:eq(8)', nRow).find('a').attr('disabled', 'disabled');
                    }

                    if (_valIsDelete == '1') {
                        $('td:eq(9)', nRow).find('a').removeAttr('disabled');
                    }
                    else {
                        $('td:eq(9)', nRow).find('a').attr('disabled', 'disabled');
                    }
                },

                "columns": [
                 { "data": "mlcNo", "autoWidth": true },
                 { "data": "patientName", "autoWidth": true },
                  {
                      "data": "Dated",
                      "type": "date",
                      "render": function (value) {
                          if (value === null) return "";

                          var pattern = /Date\(([^)]+)\)/;
                          var results = pattern.exec(value);
                          var dt = new Date(parseFloat(results[1]));
                          return dt.getDate() + "/" + (dt.getMonth() + 1) + "/" + dt.getFullYear();
                      }
                  },
                    { "data": "mlcType", "autoWidth": true },
                    { "data": "Age", "autoWidth": true },
                    { "data": "gender", "autoWidth": true },
                     { "data": "letter", "autoWidth": true },

                      {
                          "render": function (data, type, full, meta)
                          { return '<a class="btn btn-info" href="/MLCModule/MLC/Details/' + full.Id + '">Details</a>'; }
                      },
                       {
                           "render": function (data, type, full, meta)
                           { return '<a class="btn btn-info btnEdit" href="/MLCModule/MLC/Create/' + full.Id + '">Edit</a>'; }
                       },
                       {
                           data: null, render: function (data, type, row) {
                               return '<a href="javascript:void(0);" class="btn btn-danger btnDelete" onclick=DeleteData("' + row.Id + '"); >Delete</a>';
                           }
                       },
                        {
                            "render": function (data, type, full, meta)
                            { return '<a class="btn btn-info" href="/MLCModule/MLC/LoadReport?Id=' + full.Id + '">Report</a>'; }
                        },
                ]
            });
            
        });

      

        function DeleteData(mlcId) {
            if (confirm("Are you sure you want to delete ...?")) {
                Delete(mlcId);
            }
            else {
                return false;
            }
        }

        function Delete(mlcId) {
            var url = "/MLC/Delete";
            $.post(url, { Id: mlcId }, function (data) {
                if (data == "Deleted") {
                    alert("Record has been deleted!");
                    oTable = $('#demoGrid').DataTable();
                    oTable.draw();
                    window.location.href = '/MLCModule/MLC/Index'
                }
                else if (data == "Exists") {
                    alert("Record can not delete,associate with other record!");
                }
                else {
                    alert("Something went Wrong!");
                }
            });
        }

    </script>
}